{
  "name": "agenthud-proxy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BYOK pass-through CORS proxy for non-CORS OpenAI-compatible endpoints (US-6).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@agenthud/shared": "file:../shared",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "eslint-plugin-sonarjs": "^4.2.0",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.11",
    "wrangler": "^4"
  }
}
